export interface MyEvent {
  id: string
  title: string
  start: string
  end: string
}

export interface ResponseData {
  start: string
  end: string
  error: string
}

export interface IsRemoved {
  error: string
}